/**
 * App-wide constants. Bumping APP_VERSION on phase transitions helps
 * correlate logs to code state.
 */

export const APP_VERSION = '0.2.0';
export const APP_PHASE = 'Phase 2 — Real search + real playback (YouTube)';
export const APP_NAME = 'Music';

/** Search request timeout (ms). */
export const SEARCH_TIMEOUT_MS = 10_000;

/** Stream URL request timeout (ms). */
export const STREAM_TIMEOUT_MS = 12_000;
